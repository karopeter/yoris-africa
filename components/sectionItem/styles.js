import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  sectionContainer: {
       width: '100%',
       height: '100%',
     },
     sections: {
       marginTop: '30%',
       width: '100%',
       alignItems: 'center',
     },
     section: {
       fontSize: 20,
       color: 'white',
       fontWeight: '400',
     }
});

export default styles;