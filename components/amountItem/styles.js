import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  accountContainer: {
         width: '100%',
         height: '100%',
       },
       accounts: {
         marginTop: '30%',
         width: '100%',
         alignItems: 'center',
       },
       account: {
         fontSize: 20,
         color: 'white',
         fontWeight: '400',
       }
});

export default styles;